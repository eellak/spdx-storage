[project]
name = "spdx-storage"
version = "0.1.0"
description = "Storage for SPDX data"
readme = "README.md"
authors = [
    { name = "Alexios Zavras", email = "spdx@zvr.gr" }
]
requires-python = ">=3.14"
dependencies = [
    "rdflib>=7.6.0",
]

[build-system]
requires = ["uv_build>=0.11.24,<0.12.0"]
build-backend = "uv_build"
